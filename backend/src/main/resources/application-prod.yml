# 生产环境配置
server:
  port: 4001
  ssl:
    enabled: true
    key-store: classpath:ruobing.site.pfx
    key-store-type: PKCS12
    key-store-password: ey9mbf3bhvg15x3
    key-alias: ruobing.site

spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/ruobing_codebook?useUnicode=true&characterEncoding=utf-8&useSSL=false&serverTimezone=Asia/Shanghai
    username: user_ruobing_codebook
    password: Pjkluio0404.
  jackson:
    date-format: yyyy-MM-dd HH:mm:ss
    time-zone: GMT+8

# 生产环境关闭SQL日志
mybatis-plus:
  configuration:
    log-impl:

# 微信小程序（建议生产用环境变量 WXAPP_APPID / WXAPP_SECRET 覆盖，并定期轮换密钥）
wxapp:
  appid: ${WXAPP_APPID:wx5282bd68a559448f}
  secret: ${WXAPP_SECRET:df8d6f8a4f0c9b3ebcf695e95ba8d217}

logging:
  level:
    com.ruobing.codebook: info
    root: warn

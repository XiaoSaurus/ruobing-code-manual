<view class="container {{themeClass}}" style="{{pageStyle}}">
  <!-- 轮播图（动态） -->
  <view class="banner-wrap">
    <swiper class="banner-swiper"
      indicator-dots indicator-color="rgba(255,255,255,0.4)"
      indicator-active-color="#fff"
      autoplay circular interval="4000" duration="600">
      <swiper-item wx:for="{{banners}}" wx:key="id" bindtap="goBanner" data-index="{{index}}">
        <image
          class="banner-img"
          src="{{item.imageUrl || '/static/default.png'}}"
          mode="aspectFill"
          binderror="onBannerImageError"
          data-index="{{index}}"
        />
      </swiper-item>
    </swiper>
  </view>

  <!-- 热门推荐 -->
  <view class="section">
    <view class="section-header">
      <image class="section-icon" src="/static/section_icons/section-hot.png" mode="aspectFit" />
      <text class="section-title">热门推荐</text>
    </view>
    <view class="card-list">
      <view class="card" wx:for="{{hotList}}" wx:key="id" bindtap="goDetail" data-type="{{item._type}}" data-id="{{item.id}}">
        <image
          class="cover"
          src="{{item.coverImage || '/static/default.png'}}"
          mode="aspectFill"
          binderror="onHotCoverError"
          data-index="{{index}}"
        />
        <view class="card-content">
          <text class="card-title">{{item.title}}</text>
          <view class="card-footer">
            <view class="meta-item"><text class="meta-icon">👁</text><text>{{item.views || 0}}</text></view>
            <view class="meta-item"><text class="meta-icon">❤️</text><text>{{item.likes || 0}}</text></view>
            <view class="meta-item"><text class="meta-icon">⭐</text><text>{{item.favorites || 0}}</text></view>
          </view>
        </view>
      </view>
    </view>
  </view>

  <!-- 最新更新 -->
  <view class="section">
    <view class="section-header">
      <image class="section-icon" src="/static/section_icons/section-new.png" mode="aspectFit" />
      <text class="section-title">最新更新</text>
    </view>
    <view class="card-list">
      <view class="card" wx:for="{{latestList}}" wx:key="id" bindtap="goDetail" data-type="{{item._type}}" data-id="{{item.id}}">
        <image
          class="cover"
          src="{{item.coverImage || '/static/default.png'}}"
          mode="aspectFill"
          binderror="onLatestCoverError"
          data-index="{{index}}"
        />
        <view class="card-content">
          <text class="card-title">{{item.title}}</text>
          <view class="card-footer">
            <view class="meta-item"><text class="meta-icon">👁</text><text>{{item.views || 0}}</text></view>
            <view class="meta-item"><text class="meta-icon">❤️</text><text>{{item.likes || 0}}</text></view>
            <view class="meta-item"><text class="meta-icon">⭐</text><text>{{item.favorites || 0}}</text></view>
          </view>
        </view>
      </view>
    </view>
  </view>
</view>
<view class="container">
  <view class="filter-bar">
    <input class="search-input" placeholder="搜索毕业设计" value="{{keyword}}" bindinput="onKeywordInput" />
    <view class="sort-tabs">
      <text class="tab {{sortBy === 'sort_order' ? 'active' : ''}}" bindtap="onSort" data-sort="sort_order">默认</text>
      <text class="tab {{sortBy === 'views' ? 'active' : ''}}" bindtap="onSort" data-sort="views">最热</text>
      <text class="tab {{sortBy === 'latest' ? 'active' : ''}}" bindtap="onSort" data-sort="latest">最新</text>
    </view>
  </view>
  <view class="list">
    <view class="card" wx:for="{{list}}" wx:key="id" bindtap="goDetail" data-id="{{item.id}}">
      <view class="card-inner">
        <view class="info">
          <text class="title">{{item.title}}</text>
          <text class="desc">{{item.description}}</text>
          <view class="tags">
            <text class="tag" wx:for="{{item.tags.split(',')}}" wx:key="index" wx:for-item="t">{{t}}</text>
          </view>
          <view class="meta"><text>👁 {{item.views}}</text></view>
        </view>
        <image class="cover" src="{{item.coverImage || '/static/default.png'}}" mode="aspectFill" />
      </view>
    </view>
  </view>
  <view class="load-more" wx:if="{{hasMore}}"><text bindtap="loadMore">加载更多</text></view>
  <view class="no-more" wx:else><text>没有更多了</text></view>
</view>
